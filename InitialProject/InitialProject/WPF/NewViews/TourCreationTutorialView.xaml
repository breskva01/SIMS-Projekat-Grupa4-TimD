<Window x:Class="InitialProject.WPF.NewViews.TourCreationTutorialView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:InitialProject.WPF.NewViews"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid>
        <Grid x:Name="InterfaceGrid">
            <Grid.RowDefinitions>
                <RowDefinition Height="100"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Label Grid.Row="0" Content="To see tutorial for tour creation play the video below" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="25"/>
            <Border Grid.Row="1" BorderBrush="Black" BorderThickness="3" Margin="30">
                <MediaElement Grid.Row="1" x:Name="tutorialPlayer" Margin="30"/>
            </Border>
        </Grid>
    </Grid>
</Window>
